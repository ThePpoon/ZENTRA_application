# modules/heat_stroke.py — Heat Stroke / Fall Detection (stub — not yet implemented)

MP_OK = False


def on_frame(frame, meta, window_title=""):
    pass


def on_data(data, meta, frame=None):
    pass


def draw_fall_predictions(frame, predictions):
    return frame


def send_line_notify(msg, image=None, level="warning", **kwargs):
    pass
